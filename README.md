# differential-colors

Differential Bio color utilities for matplotlib and seaborn.

```python
import seaborn as sns
import differential_colors as diff

sns.set_palette(diff.palette())
diff.tooltip()
